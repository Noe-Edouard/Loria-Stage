loading:
  normalize: true
  crop: false
  target_shape: [64, 64, 64]
  input_file: null

methods:
  derivator: default
  enhancer: frangi
  segmenter: thresholding

hessian:
  mode: reflect
  cval: 0.0

processing:
  normalize: true
  parallelize: true
  chunk_size: [64, 64, 64]
  overlap_size: 8

enhancement:
  alpha: 0.5
  beta: 0.5
  gamma: 15
  scales: [2, 4, 6, 8, 10]
  scales_number: null
  scales_range: null
  black_ridges: true
  skimage: false

segmentation:
  threshold: null
